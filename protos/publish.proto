syntax = "proto3";

package protos;

message PublishBody {
    repeated string topics = 1;
    string action = 2;
    map<string, string> body = 3;
}

message PublishResponse {
    map<string, uint32> value = 1;
}

service Publish {
    rpc release (PublishBody) returns (PublishResponse);
}